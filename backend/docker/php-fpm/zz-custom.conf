[www]
; Image search / Qdrant indexing can run 2–3 minutes on cold CLIP load
request_terminate_timeout = 300
pm = dynamic
pm.max_children = 20
pm.start_servers = 4
pm.min_spare_servers = 2
pm.max_spare_servers = 8
pm.max_requests = 500
